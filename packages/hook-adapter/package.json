{
  "name": "@visual-workflows/hook-adapter",
  "version": "0.1.0",
  "description": "Tiny Claude Code hook forwarder: stdin JSON -> redact -> POST localhost bridge. Always exits 0.",
  "license": "MIT",
  "type": "module",
  "main": "./src/forward.mjs",
  "scripts": {
    "typecheck": "echo 'hook-adapter is plain JS (forward.mjs runs dependency-free) — no typecheck'"
  },
  "dependencies": {
    "@visual-workflows/protocol": "*"
  },
  "devDependencies": {
    "@types/node": "26.1.1",
    "typescript": "6.0.3"
  }
}
